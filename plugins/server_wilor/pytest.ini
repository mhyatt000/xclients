[pytest]
addopts =
    -p no:launch_testing
    -p no:launch_ros
    -p no:launch
    -p no:ros2cli_test_interfaces

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore:.*deprecated.*:DeprecationWarning
    ignore::FutureWarning

pythonpath = .
